<h1><%= header %></h1>
<ul class="nav nav-pills">
         <% for(let i=0; i<emails.length;i++) {%>
          <li class="nav-item">
            <a
              class="nav-link <% if(active==='main'){%>active<%}%>"
              <% if(active==='main'){%> aria-current="page"<%}%>
              href="<%=emails[i] %>"
              ><%=emails[i] %></a
            >
          </li>
        <%} %>
</ul>
